import React from "react";

const AddFormTitle = ({ title }) => {
  return (
    <React.Fragment>
      <h2 className="mb-2">{title}</h2>
      <div className="bordermin"></div>
    </React.Fragment>
  );
};

export default AddFormTitle;
